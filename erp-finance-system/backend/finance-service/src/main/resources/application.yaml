spring:
  application:
    name: erp-finance-service

  datasource:
    url: jdbc:postgresql://localhost:5432/erp_db
    username: postgres
    password: shayon7946
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080

jwt:
  secret: mySecretKeyForJWTTokenGenerationThatIsLongEnough12345
  expiration: 86400000